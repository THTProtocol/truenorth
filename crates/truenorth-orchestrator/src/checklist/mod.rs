//! Negative checklist module — anti-pattern verification at runtime.
//!
//! Implements the `NegativeChecklist` trait from truenorth-core, loading
//! checklist items and verifying them at configured checkpoints.

pub mod verifier;
